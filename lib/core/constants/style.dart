import 'package:flutter/animation.dart';
import 'package:flutter/material.dart';

class StyleTheme {
  StyleTheme();
  static const double textSize = 16;
  static const double titleSize = 20;
  static const double bigTitleSize = 30;
  static Color titleColor = Colors.black;
  // form
  static Color formTitleColor = Colors.grey.shade700;
  static const double formTitleSize = 12;

  static const double helperTextSize = 14;
  static Color helperTextColor = Colors.grey.shade700;
}
